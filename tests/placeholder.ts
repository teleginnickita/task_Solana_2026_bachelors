import { describe, it } from "mocha";
import { expect } from "chai";

describe("workspace scaffold", () => {
  it("documents the next implementation step", () => {
    expect(true).to.eq(true);
  });
});
